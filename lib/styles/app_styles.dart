import 'package:flutter/material.dart';

class AppStyles {
  static const BorderRadius borderRadius =
      BorderRadius.all(Radius.circular(10));
  static const paddingPage = EdgeInsets.symmetric(horizontal: 20);
}
